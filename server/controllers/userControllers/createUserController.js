const userModel = require('../../models/userModel')

async function createUserController(req,res) 
{
    // once you receive the url, then just make sure to authenticate the url and then
    try {
        
    }
    catch(error) {

    }
}